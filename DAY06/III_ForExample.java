public class III_ForExample {
  public static void main(String[] args) {
    /*
     반복문 for
      - for문은 제어조건을 한꺼번에 지정한다는 점이 다른 반복문과는 다릅니다.
      - 따라서 정확한 반복횟수를 알고 있을 때는 for문이 while문 보다 유용합니다.
      - for문도 다른 제어문과 같이 실행문장이 한불이면 블록 {}을 생략할 수 있습니다.
      - for문의 구조
          for(초기값; boolean형 조건식; 반복 표현식) {
            반복할 실행문; 
          }
     */
    /*
    [while문]
     제어번수 선언(begin)
     while (논리형 조건식(end)
      반복실행할 코드
      증김식(stop)

    [for문]
     for(제어변수선언(begin); 논리형조건식(end); 증감식(stop)) {
      반복실행할 코드
     }
     */
  }
}